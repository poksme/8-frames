import { bootstrap } from '@angular/platform-browser-dynamic'; // Angular's browser bootstrap
import { AppComponent } from './app.component'; // Our root component

bootstrap(AppComponent); // Bundle our app